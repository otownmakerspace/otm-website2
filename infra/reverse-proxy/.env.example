# ===== Reverse proxy (Traefik) environment (.env) =====
# Used by infra/reverse-proxy/reverse-proxy.yml. Copy to .env locally.

# Email Let's Encrypt registers for ACME notifications (cert expiry warnings).
CERT_EMAIL=

# Shared external Docker network. Must match SECRETS_FOLDER & app stack.
NETWORK_NAME=app_network
