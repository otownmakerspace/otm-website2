# Rendered ad outputs — re-run render.sh to regenerate.
out/

# Rasterized brand logos — generated by export-logos.sh from the brand
# logo SVGs (composed lockups + every individual mark), not source.
# Re-run export-logos.sh to regenerate.
assets/logos/

# Local scratch.
_out/
