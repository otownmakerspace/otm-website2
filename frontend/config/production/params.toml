# Production environment overlay (activated via `hugo --environment production`).
# Values here merge over _default/. No [params] header needed.
memberPortalUrl = "/login/"
memberSignupUrl = "/checkout/"
