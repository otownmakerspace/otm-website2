# Production environment overlay (activated via `hugo --environment production`).
# Values here merge over _default/. No [params] header needed.
#
# Production keeps pointing at the existing external members system. Once the
# new backend is verified on staging and DNS is flipped, this becomes "/login/"
# and the external members.theotowngarage.com retires.
memberPortalUrl = "https://members.theotowngarage.com"
