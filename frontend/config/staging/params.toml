# Staging environment overlay (activated via `hugo --environment staging`).
# The member portal lives on members.staging.makerspace.olaru.dk, served by
# the Go backend. The marketing site links to it.
memberPortalUrl = "https://members.staging.makerspace.olaru.dk/login/"
memberSignupUrl = "https://members.staging.makerspace.olaru.dk/checkout/"
